/**
 * Registro de Telas (router.js)
 *
 * Responsabilidades:
 * - registro central Views — cada módulo adiciona sua tela aqui
 * - a navegação em si (App.go / App.render) está em js/app.js
 *
 * Dependências:
 * - nenhuma (carregar antes dos módulos de tela)
 *
 * Não modificar:
 * - nome do objeto Views (usado por todos os módulos)
 */

const Views = {};
